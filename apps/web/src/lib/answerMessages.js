// Handoff error copy (profile-handoff Portfolio.dc.html genError + assistant catch).

/** Playground page genError primary line (ev-meta, #fca5a5). */
export const HANDOFF_PAGE_COMPOSE_ERROR =
  "Couldn't compose a view for that one. Try rephrasing, or pick a skill with /.";

/** Cmd+K modal pf-ask-error when the assistant request fails. */
export const HANDOFF_MODAL_UNAVAILABLE =
  "The assistant is unavailable right now. Please try again.";
